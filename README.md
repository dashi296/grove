# Grove 🌿

**Notes That Grow With You**

Groveは、思考を整理するためのパーソナルメモアプリです。シンプルに使い始めて、必要に応じて拡張できます。

現在は初期構想段階で、このリポジトリには主に設計ドキュメントが置かれています。このREADMEには、確定している設計方針と開発予定の機能をまとめています。

---

## なぜGroveか

| Obsidianの不満 | Groveの解決策 |
|---|---|
| 起動・動作が重い | Tauriベースで軽量なデスクトップ体験を目指す |
| UIが複雑すぎる | 必要な機能に絞ったシンプルUIを目指す |
| 同期が有料・面倒 | 好きなストレージをプラグインで選べる設計にする |
| AI統合が弱い | AIをプラグインとして追加できるようにする |
| モバイルUXが劣化版 | モバイルを独立設計する |

---

## 特徴

### ローカルファースト

ノートはMarkdownファイルとしてデバイスに保存する予定です。クラウドへの登録なしで使い始められ、アプリを使わなくなってもファイルが手元に残る体験を目指しています。

### シンプルなエディタ

デスクトップでは CodeMirror 6 をベースにした Markdown エディタを予定しています。モバイルは共通の command / event 契約を使いつつ、必要に応じて別実装を許容します。`[[WikiLink]]` によるノート間リンクとバックリンク生成も実装対象です。

### 高速な全文検索

SQLite FTS5による全文検索を計画しており、日本語を含むノート全体を高速に検索できるようにする方針です。

### 同期はプラグインで選ぶ

同期はデフォルトで無効にする予定です。必要になったときに、自分の環境に合ったプロバイダをプラグインとして追加できる形を目指しています。R2 や iCloud のような同期連携は本体に同梱せず、おすすめプラグインとして提示する想定です。iCloud については通常のクラウド API ではなくプラットフォームのフォルダ共有方式で扱うため、`sync-icloud` プラグインが利用する host 側の file provider 境界を整備します。

---

## 現在のステータス

- このリポジトリはまだ実装初期段階で、実装は限定的です
- モノレポ構成と各層の設計ドキュメントは作成済みですが、各 app / package の本実装はこれからです
- `CLAUDE.md` に、実装時に従うアーキテクチャ方針とコーディング規約をまとめています
- プラグイン層の設計は `plugins/README.md` にあり、プラグイン仕様のドラフトは `docs/plugin-api-spec.md` と `docs/grove-plugin-json-spec.md` にあります
- READMEにあるロードマップやプラグイン一覧は、現時点では計画であり提供済み機能ではありません

---

## 対応プラットフォーム

- **デスクトップ**: macOS / Windows / Linux
- **モバイル**: iOS / Android

---

## 同期プラグイン

同期プラグインは将来的に、設定画面のプラグインストアから追加できるようにする予定です。どのプロバイダを使っても、暗号化はアプリ側で行い、ストレージ側には暗号文のみを保存する設計を前提にしています。

### おすすめ

| プラグイン | 対応環境 | 特徴 |
|---|---|---|
| Cloudflare R2 | 全環境 | 安価・自己所有・E2E暗号化 |
| iCloud Drive | macOS / iOS のみ | Apple環境との自然な統合。クラウド API ではなく、host 側 file provider を使う `sync-icloud` プラグインとして扱う |

### コミュニティ（将来対応）

| プラグイン | 特徴 |
|---|---|
| Google Drive | Android環境で馴染みがある |
| Dropbox | ブロックレベル同期・API充実 |
| 汎用S3互換 | Wasabi・Backblaze B2・自前MinIO |
| Syncthing | P2P・クラウド不要・完全セルフホスト |

---

## ロードマップ

### v0.1 — エディタ基盤（Month 1–2）
- [ ] デスクトップ・モバイル初期化
- [ ] Markdownエディタ（デスクトップは CodeMirror 6、モバイルは別実装も許容）
- [ ] ローカルファイル読み書き
- [ ] SQLiteインデックス構築

### v0.2 — リンク・検索（Month 3）
- [ ] `[[WikiLink]]` リンクとバックリンク
- [ ] 全文検索（日本語対応）
- [ ] タグ管理

### v0.3 — 同期プラグイン（Month 4）
- [ ] プラグインストアUI
- [ ] Cloudflare R2プラグイン
- [ ] iCloud Driveプラグイン（Apple環境、フォルダ共有方式）

### v0.4 — モバイルUX（Month 5）
- [ ] モバイル専用入力UI
- [ ] クイックキャプチャ（ウィジェット）

### v1.0 — パブリックMVP（Month 6）
- [ ] AIプラグイン（Claude API）
- [ ] コミュニティプラグインドキュメント
- [ ] 公式サイト（grove.app）

---

## ブランド

- **名前**: Grove
- **サブタイトル**: Notes That Grow With You
- **カラー**: Canopy `#EAF3DE` / Leaf `#97C459` / Grove `#639922` / Bark `#27500A` / Soil `#1a1917`

---

## ライセンス

MIT
